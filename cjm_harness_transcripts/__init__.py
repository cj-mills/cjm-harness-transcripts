"""cjm-harness-transcripts: parse agent-harness session transcripts (Claude Code
JSONL first) — the record DAG, the active path, user-facing prose both sides,
and the transcript ↔ session-key mapping. Pure-parse; the graph-minting half
of the pull path lives with the projection verbs, which import this lib."""

__version__ = "0.0.1"
